# ── PC Bridge (.env) ──────────────────────────────────
# Put this file in gravity-claw/pc-bridge/.env

# Chrome remote debugging port (start Chrome with --remote-debugging-port=9222)
CDP_PORT=9222

# WebSocket server port
BRIDGE_PORT=3847

# Shared auth token (must match bot's PC_BRIDGE_TOKEN)
BRIDGE_AUTH_TOKEN=gravity-claw-bridge-2026
