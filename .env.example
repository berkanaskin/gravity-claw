# ──────────────────────────────────────────────
# Gravity Claw — Environment Configuration
# ──────────────────────────────────────────────
# Copy this file to .env and fill in your real values.
# NEVER commit .env to version control.

# Telegram Bot Token (get from @BotFather)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Google Gemini API Key (get from aistudio.google.com → "Get API Key")
MODEL_API_KEY=your-gemini-api-key-here

# Your Telegram User ID (get from @userinfobot)
TELEGRAM_ALLOWLIST_USER_ID=123456789

# Gemini model (optional, defaults to gemini-3.1-pro)
MODEL_NAME=gemini-3.1-pro

# Max agent loop iterations (optional, defaults to 10)
MAX_ITERATIONS=10

# ── Voice Input ───────────────────────────────────────────────
# TRANSCRIPTION_API_KEY=your-key   # Optional, Gemini handles it natively
# MOCK_TRANSCRIPTION=true          # For testing without API

# ── Voice Output (TTS) ───────────────────────────────────────
# TTS_API_KEY=your-google-cloud-tts-key  # Enables "reply with voice"
# MOCK_TTS=true                          # For testing without API

# ── Memory ────────────────────────────────────────────────────
# VECTOR_DB_API_KEY=your-vector-db-key   # Placeholder for future external DB
# VECTOR_DB_INDEX=your-index-name
# MOCK_MEMORY=true                       # Mock embeddings for testing

# ── MCP (External Tool Servers) ───────────────────────────────
# ENABLE_MCP=false                       # Set to false to disable MCP

# Google Workspace (Gmail + Calendar + Drive)
# Download from Google Cloud Console → Credentials → OAuth 2.0 → Desktop app
GOOGLE_CREDENTIALS_FILE=client_secret_xxxxx.apps.googleusercontent.com.json

# Notion
NOTION_API_KEY=ntn_xxxxxxxxxxxx
